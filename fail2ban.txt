fail2ban

##########################

sudo apt-get install -y python3-systemd fail2ban

ls- l /etc/fail2ban/jail.local

sudo nano /etc/fail2ban/jail.local
	[sshd]
	backend=systemd
	enabled=true
	port=22
	filter=sshd
	maxretry=3
	bantime=3600
	findtime=600

sudo systemctl restart fail2ban
sudo systemctl status fail2ban

sudo fail2ban-client status sshd
sudo iptables -L

Attack ssh@IP of this machn from another machine
After 3 wrong attempts, rejected ip will be shown


